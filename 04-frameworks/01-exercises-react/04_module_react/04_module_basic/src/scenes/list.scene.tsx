import React from "react";
import { AppLayout } from "@/layout";
import { ListPage } from "@/pods";

export const ListScene: React.FC = () => {
  return (
    <AppLayout>
      <ListPage />
    </AppLayout>
  );
};